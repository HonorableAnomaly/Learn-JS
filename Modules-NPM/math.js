// math formula variables
// const add = (x, y) => x + y;

// const subtract = (x, y) => x - y;

// const PI = 3.14159;

// const square = (x) => x * x;

// export modules
// module.exports.add = add;
// module.exports.subtract = subtract;
// module.exports.PI = PI;
// module.exports.square = square;

// exports combined into a single object and exported on one line
// const mathExports = {
//   add: add,
//   subtract: subtract,
//   PI: PI,
//   square: square,
// };

// module.exports = mathExports;

// variables added onto exports directly
// module.exports.add = (x, y) => x + y;

// module.exports.subtract = (x, y) => x - y;

// module.exports.PI = 3.14159;

// module.exports.square = (x) => x * x;

// refactored syntax
exports.add = (x, y) => x + y;

exports.subtract = (x, y) => x - y;

exports.PI = 3.14159;

exports.square = (x) => x * x;
